import type { PopoverContainerTheme } from '../PopoverContainer.vue'

const theme: PopoverContainerTheme = {
  classes: {
    wrapper: 'block w-full bg-gray-100 dark:bg-gray-800 shadow-lg border border-gray-200 dark:border-gray-800 text-sm',
  },
}

export default theme
