import type { SkeletonTheme } from '../../components/skeleton'

const theme: SkeletonTheme = {
  classes: {
    wrapper: 'animate-pulse bg-gray-300 dark:bg-gray-600 rounded-md',
  },
}

export default theme
