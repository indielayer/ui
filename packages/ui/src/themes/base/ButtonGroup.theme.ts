import type { ButtonGroupTheme } from '../../components/button'

const theme: ButtonGroupTheme = {
  classes: {
    wrapper: 'inline-flex align-middle relative -space-x-[1px]',
  },
}

export default theme
